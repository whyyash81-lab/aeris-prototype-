# AERIS simulator — PRODUCTION Firestore mode (npm run seed:prod / simulate:prod).
# Copy this to .env.prod with your real values. NEVER commit .env.prod or any
# service-account JSON.

FIREBASE_PROJECT_ID=aeris978-e62b8
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# Must stay empty in prod so the emulator path is NOT active.
FIRESTORE_EMULATOR_HOST=